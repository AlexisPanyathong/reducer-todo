export const initialState = {
    todoData: [
        {
            task: '',
            id: 123,
            completed: false
        }
    ]
};

export const reducer = (state, action) => {
    switch(action.type) {



        default:
        return state;
    }
}